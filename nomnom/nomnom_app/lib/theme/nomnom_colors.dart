import 'package:flutter/material.dart';

class NomNomColors {
  static const primary = Color(0xFFFF7A3C);      // warm orange
  static const primaryDeep = Color(0xFFE65A1F);  // stronger accent

  static const secondary = Color(0xFF3FA87A);        // sage green
  static const secondaryLight = Color(0xFFC9F0D4);   // light green

  static const background = Color(0xFFFFF7F2); // warm off-white
  static const surface = Color(0xFFFFFFFF);    // cards, appbars

  static const textPrimary = Color(0xFF2B1B18);
  static const textSecondary = Color(0xFF6F554C);

  static const border = Color(0xFFE6D5CC);

  static const error = Color(0xFFE53935);

  static const darkBackground = Color(0xFF111418); // main scaffold
  static const darkSurface = Color(0xFF181C22);    // cards, sheets

  static const darkPrimary = Color(0xFFFFC857);    // warm amber
  static const darkPrimaryDeep = Color(0xFFE0A835);

  static const darkSecondary = Color(0xFFA23B72);      // berry
  static const darkSecondaryContainer = Color(0xFF2A1D2D);

  static const darkTextPrimary = Color(0xFFF5F5F5);
  static const darkTextSecondary = Color(0xFFB0B8C3);

  static const darkBorder = Color(0xFF2A3038);

  static const darkError = Color(0xFFEF5350);
}
